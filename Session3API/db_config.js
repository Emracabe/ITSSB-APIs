const config = {
    server: "localhost",
    user: "sa",
    password: "itssb09",
    database: 'session3final',
    options: {
        trustServerCertificate: true,
    }
};

module.exports = config;